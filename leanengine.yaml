runtime: static
install:
  - ls ../app
  - ls ../transformed
  - cat package.json
  - npm install pnpm@latest --force --omit=dev
  - cat package.json
  - npx pnpm -v
  - npx pnpm install --no-frozen-lockfile
